{
  "name": "qiniu-files-manager",
  "version": "2.1.6",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:client": "pnpm -r --parallel --filter client run dev",
    "dev:server": "pnpm -r --parallel --filter server run start:dev",
    "start:prod": "pnpm -r --parallel --filter server run start:prod",
    "start:pm2": "pnpm -r --parallel --filter server run start:pm2",
    "build": "pnpm -r --parallel --filter='./packages/*' run build",
    "lint": "pnpm -r --parallel --filter='./packages/*' run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
